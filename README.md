# cli
Command line tool for pushing code frames to api

To get started:

```bash
git clone https://github.com/iterableio/cli.git
cd cli
virtualenv env
. env/bin/activate
make bootstrap
```

Good to go
